{
  "category": "enumeration",
  "description": "Network service authentication attacks using THC Hydra",
  "commands": [
    {
      "id": "hydra-ssh-single-user",
      "name": "SSH Dictionary Attack - Single User",
      "category": "enumeration",
      "command": "hydra -l <USERNAME> -P <WORDLIST> -s <PORT> ssh://<TARGET>",
      "description": "Dictionary attack against SSH service for single user",
      "tags": ["HYDRA", "SSH", "DICTIONARY_ATTACK", "PASSWORD_ATTACK", "OSCP:HIGH", "NETWORK_AUTH"],
      "variables": [
        {
          "name": "<USERNAME>",
          "description": "Target username for authentication",
          "example": "george",
          "required": true
        },
        {
          "name": "<WORDLIST>",
          "description": "Path to password wordlist",
          "example": "/usr/share/wordlists/rockyou.txt",
          "required": true
        },
        {
          "name": "<PORT>",
          "description": "SSH port (default 22, often non-standard)",
          "example": "2222",
          "required": false
        },
        {
          "name": "<TARGET>",
          "description": "Target IP address or hostname",
          "example": "192.168.45.100",
          "required": true
        }
      ],
      "flag_explanations": {
        "-l": "Login name - Specifies single username to test. Use when you've identified a specific user through enumeration (LDAP, email addresses, default accounts like 'root' or 'admin'). More focused than username list attacks.",
        "-P": "Password list - Path to wordlist file containing password candidates. rockyou.txt (14M passwords) is standard starting point. More effective with rules applied first. Consider target's password policy when selecting wordlist.",
        "-s": "Port - Override default service port. REQUIRED when service runs on non-standard port (common in CTF/lab environments to avoid automated attacks). SSH defaults to 22 but often moved to 2222, 22222, etc.",
        "ssh://": "Protocol specification - Tells Hydra to use SSH authentication module. Format: protocol://target or target protocol"
      },
      "success_indicators": [
        "[22][ssh] host: <TARGET>   login: <USERNAME>   password: <PASSWORD>",
        "1 of 1 target successfully completed, 1 valid password found",
        "valid password found"
      ],
      "failure_indicators": [
        "0 of 1 target completed, 0 valid password found",
        "Connection refused",
        "Too many authentication failures",
        "Permission denied",
        "Network unreachable"
      ],
      "next_steps": [
        "ssh-login-verify",
        "password-reuse-test",
        "credential-spray-rdp"
      ],
      "alternatives": [
        "medusa -h <TARGET> -u <USERNAME> -P <WORDLIST> -M ssh",
        "ncrack -p 22 -user <USERNAME> -P <WORDLIST> <TARGET>",
        "Manual: for password in $(cat wordlist.txt); do sshpass -p \"$password\" ssh <USERNAME>@<TARGET>; done"
      ],
      "prerequisites": [
        "nmap-ssh-detect"
      ],
      "troubleshooting": {
        "Too many authentication failures": "SSH configured with MaxAuthTries limit. Solution: Use -t 4 to limit parallel tasks, or wait between attempts with -w",
        "Connection refused": "Service not running or firewall blocking. Verify with: nmap -p <PORT> <TARGET>",
        "No password found after hours": "1) Verify username is correct 2) Check password policy requirements 3) Try wordlist with rules (mutated passwords) 4) Consider time constraints - may not be viable attack vector"
      },
      "notes": "OSCP TIP: Dictionary attacks generate significant noise (logs, IDS alerts). Always verify target doesn't have account lockout policies before running. Test with small wordlist first (100 passwords). Time estimate: 14M passwords @ 16 tasks/second = ~14 hours for rockyou.txt on typical lab connection. Use filtered wordlists or rule-based mutations for efficiency. Default accounts (root, admin, administrator) are high-value targets.",
      "oscp_relevance": "high"
    },
    {
      "id": "hydra-ssh-user-list",
      "name": "SSH Dictionary Attack - User List",
      "category": "enumeration",
      "command": "hydra -L <USERLIST> -P <WORDLIST> -s <PORT> ssh://<TARGET>",
      "description": "Dictionary attack against SSH with multiple usernames",
      "tags": ["HYDRA", "SSH", "DICTIONARY_ATTACK", "PASSWORD_ATTACK", "OSCP:MEDIUM", "NETWORK_AUTH"],
      "variables": [
        {
          "name": "<USERLIST>",
          "description": "Path to file containing usernames (one per line)",
          "example": "/usr/share/wordlists/metasploit/unix_users.txt",
          "required": true
        },
        {
          "name": "<WORDLIST>",
          "description": "Path to password wordlist",
          "example": "/usr/share/wordlists/rockyou.txt",
          "required": true
        },
        {
          "name": "<PORT>",
          "description": "SSH port",
          "example": "22",
          "required": false
        },
        {
          "name": "<TARGET>",
          "description": "Target IP address",
          "example": "192.168.45.100",
          "required": true
        }
      ],
      "flag_explanations": {
        "-L": "User list - Path to file with multiple usernames. Use capital 'L' for file, lowercase 'l' for single user. Effective when you've enumerated users through SMTP VRFY, LDAP, or web application user disclosure. Increases keyspace multiplicatively (users × passwords)."
      },
      "success_indicators": [
        "valid password found",
        "[<PORT>][ssh] host: <TARGET>   login: <USER>   password: <PASS>"
      ],
      "failure_indicators": [
        "0 valid passwords found",
        "Connection timed out"
      ],
      "notes": "WARNING: This attack can trigger account lockouts if multiple users have lockout policies. Keyspace = userlist_size × wordlist_size. With 100 users and 14M passwords = 1.4B attempts. Consider using focused wordlists or password spraying instead.",
      "oscp_relevance": "medium"
    },
    {
      "id": "hydra-rdp-spray",
      "name": "RDP Password Spraying",
      "category": "enumeration",
      "command": "hydra -L <USERLIST> -p \"<PASSWORD>\" rdp://<TARGET>",
      "description": "Password spraying attack against RDP service",
      "tags": ["HYDRA", "RDP", "PASSWORD_SPRAY", "PASSWORD_ATTACK", "OSCP:HIGH", "WINDOWS"],
      "variables": [
        {
          "name": "<USERLIST>",
          "description": "Path to file with usernames",
          "example": "/usr/share/wordlists/dirb/others/names.txt",
          "required": true
        },
        {
          "name": "<PASSWORD>",
          "description": "Single password to test against all users",
          "example": "SuperS3cure1337#",
          "required": true
        },
        {
          "name": "<TARGET>",
          "description": "Target IP address",
          "example": "192.168.45.100",
          "required": true
        }
      ],
      "flag_explanations": {
        "-p": "Password - Single password (lowercase 'p'). Used for password spraying. Lowercase 'p' = single password, capital 'P' = password list. Spray attacks test one password against many users, avoiding account lockout thresholds (typically 3-5 failed attempts per account).",
        "rdp://": "RDP protocol - Uses FreeRDP for authentication testing. May show '[ERROR] freerdp: The connection failed to establish' even on successful authentication (Hydra limitation). Verify credentials manually with: xfreerdp /v:<TARGET> /u:<USER> /p:<PASS>"
      },
      "success_indicators": [
        "[3389][rdp] host: <TARGET>   login: <USER>   password: <PASS>",
        "valid password found"
      ],
      "failure_indicators": [
        "[ERROR] freerdp: The connection failed to establish",
        "Account locked",
        "Connection refused"
      ],
      "next_steps": [
        "rdp-login-verify",
        "test-password-reuse-ssh"
      ],
      "troubleshooting": {
        "ERROR freerdp shown for valid creds": "Known Hydra limitation with RDP module. Solution: Verify manually with xfreerdp or rdesktop",
        "Slow performance": "RDP authentication is slow by design. Solution: Limit threads with -t 4, be patient"
      },
      "notes": "OSCP TIP: Password spraying is stealthier than dictionary attacks (fewer attempts per user, less likely to trigger lockouts). Common spray passwords: Company name + year + !, Season + year + !, Password123!, Welcome123!. RDP default port 3389 is often exposed on Windows servers. Time estimate: ~15 minutes for 8,000 users with 1 password.",
      "oscp_relevance": "high"
    },
    {
      "id": "hydra-http-post-form",
      "name": "HTTP POST Login Form Attack",
      "category": "enumeration",
      "command": "hydra -l <USERNAME> -P <WORDLIST> <TARGET> http-post-form \"<LOGIN_PAGE>:<POST_DATA>:<FAIL_STRING>\"",
      "description": "Dictionary attack against HTTP POST login forms",
      "tags": ["HYDRA", "WEB", "HTTP", "POST", "LOGIN_FORM", "PASSWORD_ATTACK", "OSCP:HIGH"],
      "variables": [
        {
          "name": "<USERNAME>",
          "description": "Username to test",
          "example": "admin",
          "required": true
        },
        {
          "name": "<WORDLIST>",
          "description": "Password wordlist path",
          "example": "/usr/share/wordlists/rockyou.txt",
          "required": true
        },
        {
          "name": "<TARGET>",
          "description": "Target IP or hostname (no protocol)",
          "example": "192.168.45.100",
          "required": true
        },
        {
          "name": "<LOGIN_PAGE>",
          "description": "Path to login page (from Burp intercept)",
          "example": "/index.php",
          "required": true
        },
        {
          "name": "<POST_DATA>",
          "description": "POST request body with ^PASS^ placeholder (from Burp)",
          "example": "fm_usr=user&fm_pwd=^PASS^",
          "required": true
        },
        {
          "name": "<FAIL_STRING>",
          "description": "String indicating failed login (from browser)",
          "example": "Login failed. Invalid",
          "required": true
        }
      ],
      "flag_explanations": {
        "http-post-form": "HTTP POST method - Format: \"<page>:<params>:<fail>\". Three colon-separated fields REQUIRED. Hydra submits POST data with ^PASS^ replaced by wordlist passwords. ^USER^ also supported for username placeholder.",
        "<POST_DATA>": "Request body - Captured from Burp Suite intercept. Include all form fields (username, password, tokens). Use ^PASS^ where password goes, ^USER^ where username goes (if testing both).",
        "<FAIL_STRING>": "Failure indicator - Shortened string from failed login response. Hydra searches response for this string. If NOT found = successful login. Keep it short but unique (avoid generic words like 'password' or 'username' to reduce false positives)."
      },
      "success_indicators": [
        "[80][http-post-form] host: <TARGET>   login: <USER>   password: <PASS>",
        "1 valid password found"
      ],
      "failure_indicators": [
        "0 valid passwords found",
        "Page moved permanently",
        "Connection failed"
      ],
      "prerequisites": [
        "burp-intercept-login"
      ],
      "next_steps": [
        "manual-login-verify",
        "session-cookie-capture"
      ],
      "alternatives": [
        "wfuzz -c -z file,<WORDLIST> -d \"fm_usr=<USER>&fm_pwd=FUZZ\" --hs \"<FAIL_STRING>\" http://<TARGET>/<PAGE>",
        "ffuf -w <WORDLIST>:FUZZ -X POST -d \"fm_usr=<USER>&fm_pwd=FUZZ\" -H \"Content-Type: application/x-www-form-urlencoded\" -u http://<TARGET>/<PAGE> -fr \"<FAIL_STRING>\""
      ],
      "troubleshooting": {
        "False positives": "Failure string too generic. Solution: Use longer, more specific string from failed login message",
        "No matches found": "Failure string not in response. Solution: Check response in Burp, verify exact error message",
        "Slow performance": "Web app may have rate limiting. Solution: Reduce threads with -t 4"
      },
      "notes": "OSCP TIP: 1) Use Burp to intercept login attempt - get POST data 2) Try failed login in browser - get failure message 3) Shorten failure string (avoid words like 'password' or 'username') 4) Default web users: admin, administrator, user, root, test. Time estimate: 14M passwords @ ~10-20 attempts/second = 16-32 hours (slow due to HTTP overhead). Use filtered wordlists. Web Application Firewalls (WAFs) will block this quickly - may not be viable if WAF present.",
      "oscp_relevance": "high"
    },
    {
      "id": "hydra-ftp-attack",
      "name": "FTP Dictionary Attack",
      "category": "enumeration",
      "command": "hydra -l <USERNAME> -P <WORDLIST> ftp://<TARGET>",
      "description": "Dictionary attack against FTP service",
      "tags": ["HYDRA", "FTP", "DICTIONARY_ATTACK", "PASSWORD_ATTACK", "OSCP:MEDIUM", "FILE_TRANSFER"],
      "variables": [
        {
          "name": "<USERNAME>",
          "description": "FTP username",
          "example": "ftp",
          "required": true
        },
        {
          "name": "<WORDLIST>",
          "description": "Password wordlist",
          "example": "/usr/share/wordlists/rockyou.txt",
          "required": true
        },
        {
          "name": "<TARGET>",
          "description": "Target IP",
          "example": "192.168.45.100",
          "required": true
        }
      ],
      "flag_explanations": {
        "ftp://": "FTP protocol - Default port 21. Hydra tests cleartext authentication. Check for anonymous login first (ftp/anonymous user) before running dictionary attack."
      },
      "success_indicators": [
        "[21][ftp] host: <TARGET>   login: <USER>   password: <PASS>"
      ],
      "failure_indicators": [
        "0 valid passwords found",
        "530 Login incorrect"
      ],
      "prerequisites": [
        "nmap-ftp-detect",
        "ftp-anonymous-test"
      ],
      "notes": "Check anonymous FTP first: ftp <TARGET>, username 'anonymous', password blank or email. FTP cleartext = fast authentication = faster than HTTP/RDP. Default users: ftp, anonymous, admin, root.",
      "oscp_relevance": "medium"
    },
    {
      "id": "hydra-smb-attack",
      "name": "SMB Dictionary Attack",
      "category": "enumeration",
      "command": "hydra -l <USERNAME> -P <WORDLIST> smb://<TARGET>",
      "description": "Dictionary attack against SMB/CIFS service",
      "tags": ["HYDRA", "SMB", "DICTIONARY_ATTACK", "PASSWORD_ATTACK", "OSCP:HIGH", "WINDOWS"],
      "variables": [
        {
          "name": "<USERNAME>",
          "description": "Windows username",
          "example": "Administrator",
          "required": true
        },
        {
          "name": "<WORDLIST>",
          "description": "Password wordlist",
          "example": "/usr/share/wordlists/rockyou.txt",
          "required": true
        },
        {
          "name": "<TARGET>",
          "description": "Target IP",
          "example": "192.168.45.100",
          "required": true
        }
      ],
      "flag_explanations": {
        "smb://": "SMB protocol - Tests authentication on port 445 (SMB) or 139 (NetBIOS). Windows account authentication. Successful auth doesn't guarantee share access (depends on ACLs)."
      },
      "success_indicators": [
        "[445][smb] host: <TARGET>   login: <USER>   password: <PASS>"
      ],
      "failure_indicators": [
        "NT_STATUS_LOGON_FAILURE",
        "NT_STATUS_ACCOUNT_LOCKED_OUT",
        "0 valid passwords found"
      ],
      "next_steps": [
        "smbclient-list-shares",
        "smbmap-enumerate",
        "crackmapexec-spray"
      ],
      "alternatives": [
        "crackmapexec smb <TARGET> -u <USER> -p <WORDLIST>",
        "smbclient -U <USER> -L //<TARGET> (manual testing)"
      ],
      "troubleshooting": {
        "NT_STATUS_ACCOUNT_LOCKED_OUT": "Account lockout policy triggered. STOP IMMEDIATELY. Wait 30-60 minutes or test different account. Lockout typically 3-5 failed attempts.",
        "Connection refused on 445": "SMB port filtered. Try port 139: hydra -l <USER> -P <WORDLIST> -s 139 smb://<TARGET>"
      },
      "notes": "OSCP TIP: Windows default accounts - Administrator, Guest (usually disabled), service accounts. SMB lockout policies are STRICT - be very careful. Test with 5-10 passwords first. Prefer password spraying over dictionary attack on SMB.",
      "oscp_relevance": "high"
    },
    {
      "id": "hydra-mysql-attack",
      "name": "MySQL Dictionary Attack",
      "category": "enumeration",
      "command": "hydra -l <USERNAME> -P <WORDLIST> mysql://<TARGET>",
      "description": "Dictionary attack against MySQL database",
      "tags": ["HYDRA", "MYSQL", "DATABASE", "DICTIONARY_ATTACK", "PASSWORD_ATTACK", "OSCP:MEDIUM"],
      "variables": [
        {
          "name": "<USERNAME>",
          "description": "MySQL username",
          "example": "root",
          "required": true
        },
        {
          "name": "<WORDLIST>",
          "description": "Password wordlist",
          "example": "/usr/share/wordlists/rockyou.txt",
          "required": true
        },
        {
          "name": "<TARGET>",
          "description": "Target IP",
          "example": "192.168.45.100",
          "required": true
        }
      ],
      "flag_explanations": {
        "mysql://": "MySQL protocol - Default port 3306. Tests database authentication. Common users: root, admin, user, mysql. Success gives database access for data exfiltration or privilege escalation via UDF."
      },
      "success_indicators": [
        "[3306][mysql] host: <TARGET>   login: <USER>   password: <PASS>"
      ],
      "failure_indicators": [
        "Access denied for user",
        "Connection refused"
      ],
      "next_steps": [
        "mysql-login-verify",
        "mysql-enumerate-databases",
        "mysql-udf-exploit"
      ],
      "notes": "Default MySQL users: root (no password on some installs), admin, user. Port 3306 rarely exposed externally but common internally. Successful auth = database access = potential data exfiltration or privilege escalation.",
      "oscp_relevance": "medium"
    },
    {
      "id": "hydra-verbose",
      "name": "Hydra Verbose Output",
      "category": "enumeration",
      "command": "hydra -l <USERNAME> -P <WORDLIST> -V <PROTOCOL>://<TARGET>",
      "description": "Run Hydra with verbose output showing every attempt",
      "tags": ["HYDRA", "VERBOSE", "DEBUG", "PASSWORD_ATTACK", "OSCP:LOW"],
      "variables": [
        {
          "name": "<USERNAME>",
          "description": "Target username",
          "example": "admin",
          "required": true
        },
        {
          "name": "<WORDLIST>",
          "description": "Password wordlist",
          "example": "/usr/share/wordlists/rockyou.txt",
          "required": true
        },
        {
          "name": "<PROTOCOL>",
          "description": "Protocol to attack",
          "example": "ssh",
          "required": true
        },
        {
          "name": "<TARGET>",
          "description": "Target IP",
          "example": "192.168.45.100",
          "required": true
        }
      ],
      "flag_explanations": {
        "-V": "Verbose mode - Shows every login attempt with username:password combination. Useful for debugging (verify wordlist loaded, confirm attempts being made). WARNING: Extremely noisy output - can fill screen/logs quickly. Use only for troubleshooting or small wordlists."
      },
      "success_indicators": [
        "[ATTEMPT] target <TARGET> - login <USER> - pass <PASS>",
        "valid password found"
      ],
      "notes": "Use for troubleshooting only. Output is overwhelming with large wordlists. Alternative: -v (lowercase) shows found passwords without showing every attempt.",
      "oscp_relevance": "low"
    },
    {
      "id": "hydra-custom-port",
      "name": "Hydra Custom Port",
      "category": "enumeration",
      "command": "hydra -l <USERNAME> -P <WORDLIST> -s <PORT> <PROTOCOL>://<TARGET>",
      "description": "Attack service on non-standard port",
      "tags": ["HYDRA", "CUSTOM_PORT", "PASSWORD_ATTACK", "OSCP:MEDIUM"],
      "variables": [
        {
          "name": "<USERNAME>",
          "description": "Username",
          "example": "admin",
          "required": true
        },
        {
          "name": "<WORDLIST>",
          "description": "Password wordlist",
          "example": "/usr/share/wordlists/rockyou.txt",
          "required": true
        },
        {
          "name": "<PORT>",
          "description": "Custom port number",
          "example": "2222",
          "required": true
        },
        {
          "name": "<PROTOCOL>",
          "description": "Service protocol",
          "example": "ssh",
          "required": true
        },
        {
          "name": "<TARGET>",
          "description": "Target IP",
          "example": "192.168.45.100",
          "required": true
        }
      ],
      "flag_explanations": {
        "-s": "Custom port - Override default service port. REQUIRED when service runs on non-standard port. Common in CTF/labs to avoid automated scanners. SSH often on 2222/22222, HTTP on 8080/8000/8888, RDP on 3390."
      },
      "notes": "Always check nmap results for non-standard ports. Examples: SSH on 2222, HTTP on 8080, RDP on 3390, MySQL on 13306.",
      "oscp_relevance": "medium"
    },
    {
      "id": "hydra-parallel-tasks",
      "name": "Hydra Parallel Tasks Configuration",
      "category": "enumeration",
      "command": "hydra -l <USERNAME> -P <WORDLIST> -t <TASKS> <PROTOCOL>://<TARGET>",
      "description": "Configure number of parallel connections",
      "tags": ["HYDRA", "PERFORMANCE", "THREADING", "PASSWORD_ATTACK", "OSCP:MEDIUM"],
      "variables": [
        {
          "name": "<USERNAME>",
          "description": "Username",
          "example": "admin",
          "required": true
        },
        {
          "name": "<WORDLIST>",
          "description": "Password wordlist",
          "example": "/usr/share/wordlists/rockyou.txt",
          "required": true
        },
        {
          "name": "<TASKS>",
          "description": "Number of parallel tasks",
          "example": "4",
          "required": true
        },
        {
          "name": "<PROTOCOL>",
          "description": "Service protocol",
          "example": "ssh",
          "required": true
        },
        {
          "name": "<TARGET>",
          "description": "Target IP",
          "example": "192.168.45.100",
          "required": true
        }
      ],
      "flag_explanations": {
        "-t": "Parallel tasks - Number of concurrent connections. Default 16 (aggressive). Lower values (4-8) reduce load on target and risk of triggering IDS/lockout. Higher values (32-64) faster but riskier. Balance speed vs stealth based on target sensitivity."
      },
      "success_indicators": [
        "max <TASKS> tasks per 1 server",
        "valid password found"
      ],
      "troubleshooting": {
        "Too many authentication failures": "SSH MaxAuthTries limit. Solution: Reduce with -t 4",
        "Connection timeouts": "Target overwhelmed. Solution: Reduce to -t 2-4",
        "Account lockouts": "Too many failed attempts. Solution: Use -t 1 with -w 30 (30 second wait between attempts)"
      },
      "notes": "OSCP TIP: Default -t 16 is aggressive - may trigger lockouts or 'too many authentication failures' on SSH. Conservative approach: -t 4 for SSH, -t 8 for HTTP, -t 16 for FTP (fast protocol). Always test with small wordlist first.",
      "oscp_relevance": "medium"
    },
    {
      "id": "hydra-wait-time",
      "name": "Hydra Wait Time Between Attempts",
      "category": "enumeration",
      "command": "hydra -l <USERNAME> -P <WORDLIST> -t <TASKS> -w <SECONDS> <PROTOCOL>://<TARGET>",
      "description": "Add delay between connection attempts to avoid detection/lockout",
      "tags": ["HYDRA", "STEALTH", "EVASION", "PASSWORD_ATTACK", "OSCP:LOW"],
      "variables": [
        {
          "name": "<USERNAME>",
          "description": "Username",
          "example": "admin",
          "required": true
        },
        {
          "name": "<WORDLIST>",
          "description": "Password wordlist",
          "example": "/usr/share/wordlists/rockyou.txt",
          "required": true
        },
        {
          "name": "<TASKS>",
          "description": "Parallel tasks",
          "example": "2",
          "required": true
        },
        {
          "name": "<SECONDS>",
          "description": "Seconds to wait between attempts",
          "example": "5",
          "required": true
        },
        {
          "name": "<PROTOCOL>",
          "description": "Service protocol",
          "example": "ssh",
          "required": true
        },
        {
          "name": "<TARGET>",
          "description": "Target IP",
          "example": "192.168.45.100",
          "required": true
        }
      ],
      "flag_explanations": {
        "-w": "Wait time - Seconds to wait between connection attempts. Slows attack but reduces detection risk and avoids overwhelming target. Use when: 1) IDS/IPS present 2) Account lockout policy strict 3) Target resource-constrained. Dramatically increases total attack time."
      },
      "notes": "Stealth option - significantly slows attack. Example: 1000 passwords × 5 seconds = 83 minutes. Only use when absolutely necessary (strict lockout, IDS alerts, exam restriction). Not typically needed in OSCP labs.",
      "oscp_relevance": "low"
    },
    {
      "id": "hydra-resume-session",
      "name": "Hydra Resume Session",
      "category": "enumeration",
      "command": "hydra -l <USERNAME> -P <WORDLIST> -R <PROTOCOL>://<TARGET>",
      "description": "Resume previously aborted/crashed Hydra session",
      "tags": ["HYDRA", "RESUME", "SESSION", "PASSWORD_ATTACK", "OSCP:LOW"],
      "variables": [
        {
          "name": "<USERNAME>",
          "description": "Username (must match original session)",
          "example": "admin",
          "required": true
        },
        {
          "name": "<WORDLIST>",
          "description": "Password wordlist (must match original)",
          "example": "/usr/share/wordlists/rockyou.txt",
          "required": true
        },
        {
          "name": "<PROTOCOL>",
          "description": "Protocol (must match original)",
          "example": "ssh",
          "required": true
        },
        {
          "name": "<TARGET>",
          "description": "Target IP (must match original)",
          "example": "192.168.45.100",
          "required": true
        }
      ],
      "flag_explanations": {
        "-R": "Resume - Restores session from hydra.restore file (automatically created). Continues from last attempted password. Useful for long-running attacks interrupted by network issues, crashes, or Ctrl+C. All parameters (user, wordlist, target, protocol) must match original session exactly."
      },
      "success_indicators": [
        "Restoring session from file",
        "Resuming at position"
      ],
      "failure_indicators": [
        "No session file found",
        "Session file incompatible"
      ],
      "notes": "Hydra creates hydra.restore file automatically. Resume with -R flag. All original parameters must match. Useful for multi-hour attacks that get interrupted.",
      "oscp_relevance": "low"
    },
    {
      "id": "hydra-show-attempts",
      "name": "Hydra Show Attempt Progress",
      "category": "enumeration",
      "command": "hydra -l <USERNAME> -P <WORDLIST> -v <PROTOCOL>://<TARGET>",
      "description": "Show found credentials without verbose attempt output",
      "tags": ["HYDRA", "OUTPUT", "PASSWORD_ATTACK", "OSCP:MEDIUM"],
      "variables": [
        {
          "name": "<USERNAME>",
          "description": "Username",
          "example": "admin",
          "required": true
        },
        {
          "name": "<WORDLIST>",
          "description": "Password wordlist",
          "example": "/usr/share/wordlists/rockyou.txt",
          "required": true
        },
        {
          "name": "<PROTOCOL>",
          "description": "Service protocol",
          "example": "ssh",
          "required": true
        },
        {
          "name": "<TARGET>",
          "description": "Target IP",
          "example": "192.168.45.100",
          "required": true
        }
      ],
      "flag_explanations": {
        "-v": "Verbose (lowercase) - Shows successful credentials and status updates without showing every attempt (unlike -V uppercase). Provides progress feedback without overwhelming output. Best for production use."
      },
      "notes": "Lowercase -v is recommended (shows progress without spam). Uppercase -V shows every attempt (too noisy). Default (no flag) only shows final results.",
      "oscp_relevance": "medium"
    },
    {
      "id": "hydra-output-file",
      "name": "Hydra Save Output to File",
      "category": "enumeration",
      "command": "hydra -l <USERNAME> -P <WORDLIST> -o <OUTPUT_FILE> <PROTOCOL>://<TARGET>",
      "description": "Save found credentials to file for documentation",
      "tags": ["HYDRA", "OUTPUT", "LOGGING", "PASSWORD_ATTACK", "OSCP:MEDIUM"],
      "variables": [
        {
          "name": "<USERNAME>",
          "description": "Username",
          "example": "admin",
          "required": true
        },
        {
          "name": "<WORDLIST>",
          "description": "Password wordlist",
          "example": "/usr/share/wordlists/rockyou.txt",
          "required": true
        },
        {
          "name": "<OUTPUT_FILE>",
          "description": "Path to output file",
          "example": "hydra_results.txt",
          "required": true
        },
        {
          "name": "<PROTOCOL>",
          "description": "Service protocol",
          "example": "ssh",
          "required": true
        },
        {
          "name": "<TARGET>",
          "description": "Target IP",
          "example": "192.168.45.100",
          "required": true
        }
      ],
      "flag_explanations": {
        "-o": "Output file - Save found credentials to file. Format: 'host: <IP> login: <USER> password: <PASS>'. Critical for OSCP documentation requirements. File created automatically. Credentials still shown on screen."
      },
      "notes": "OSCP REQUIREMENT: Document all found credentials with source/method. -o flag creates audit trail. Always use for exam/professional work.",
      "oscp_relevance": "medium"
    },
    {
      "id": "hydra-combo-attack",
      "name": "Hydra Credential Pair Attack",
      "category": "enumeration",
      "command": "hydra -C <CREDENTIAL_FILE> <PROTOCOL>://<TARGET>",
      "description": "Test credential pairs from file (username:password format)",
      "tags": ["HYDRA", "CREDENTIAL_STUFFING", "PASSWORD_ATTACK", "OSCP:MEDIUM"],
      "variables": [
        {
          "name": "<CREDENTIAL_FILE>",
          "description": "File with username:password pairs (one per line)",
          "example": "creds.txt",
          "required": true
        },
        {
          "name": "<PROTOCOL>",
          "description": "Service protocol",
          "example": "ssh",
          "required": true
        },
        {
          "name": "<TARGET>",
          "description": "Target IP",
          "example": "192.168.45.100",
          "required": true
        }
      ],
      "flag_explanations": {
        "-C": "Credential pairs - File format: 'username:password' (colon-separated, one pair per line). Used for credential stuffing attacks (testing leaked credentials from breaches, default creds lists). More efficient than separate user/password lists when you have known pairs. Example file: admin:admin, root:toor, user:password123"
      },
      "success_indicators": [
        "valid password found"
      ],
      "notes": "Use for default credential testing or credential stuffing. Common default pairs: admin:admin, root:root, administrator:password, user:user. SecLists has good default credential lists.",
      "oscp_relevance": "medium"
    }
  ]
}
